export default interface IProduct {
  id?: string,
  name: string,
  subName?: string,
  manufacturer: string,
  unitSelling: string,
  category: string,
  code: string,
  unitMeasure?: string,
  size?: number,
  image?: string,
}